import 'package:flutter/material.dart';

const Color labelText = Color.fromRGBO(180, 180, 180, 1);
const Color primaryColor = Color.fromRGBO(24, 24, 24, 1);
const Color appBarBg = Color.fromRGBO(48, 48, 48, 1);

// Clock colors
const Color whiteClockActiveBg = Colors.white;
const Color blackClockActiveBg = Colors.black;
const Color clockInactiveBg = Color.fromRGBO(29, 29, 29, 1);

const Color whiteClockActiveText = primaryColor;
const Color whiteClockInactiveText = labelText;

const Color blackClockActiveText = labelText;
const Color blackClockInactiveText = labelText;
